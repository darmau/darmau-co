# 构建期变量（Astro 静态构建时读取，见 astro.config.mjs 的 envField）。
#
# 三个值都不是密钥，所以直接进仓库——本地、CI、任何干净检出都能构建：
#   SUPABASE_ANON_KEY 是 anon 角色，受 RLS 保护，本来就会随浏览器 JS 发出去
#   SUPABASE_URL / IMG_PREFIX 就是公开的接口和图床地址
#
# 真正的密钥不要写这里。本地覆盖写 .env.local（仍被 gitignore）。

SUPABASE_URL=https://uvehzviuyezvgbwmghzk.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InV2ZWh6dml1eWV6dmdid21naHprIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MTU3Njk3OTEsImV4cCI6MjAzMTM0NTc5MX0.WQ4kOirpsfjpHefEWiysrR9MLgMDbY_1_WIqifULBjM
IMG_PREFIX=https://img.darmau.design/
