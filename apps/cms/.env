# 构建期变量（SvelteKit $env/static/*，vite build 时内联进产物）。
#
# 这四个值都不是密钥，所以直接进仓库——本地、CI、Cloudflare Workers Builds
# 三处构建都不用再单独配一遍，也不会因为控制台漏配而构建失败：
#   PUBLIC_* 本来就会随浏览器 JS 发出去（SvelteKit 的 PUBLIC_ 前缀就是这个含义）
#   URL_PREFIX / BASE_URL 就是站点自己的公开地址
#
# 真正的密钥不要写这里。运行时密钥走 `wrangler secret put`，
# 本地覆盖写 .env.local（已 gitignore，优先级高于本文件）。

PUBLIC_SUPABASE_URL=https://uvehzviuyezvgbwmghzk.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InV2ZWh6dml1eWV6dmdid21naHprIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MTU3Njk3OTEsImV4cCI6MjAzMTM0NTc5MX0.WQ4kOirpsfjpHefEWiysrR9MLgMDbY_1_WIqifULBjM
URL_PREFIX=https://img.darmau.co
BASE_URL=https://darmau.co
